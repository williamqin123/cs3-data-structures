import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

public class BinaryCardTreeDriver {

    public static void read(Scanner sc, BinaryCardTree tree) {
        for (int i = 0; i < Integer.parseInt(sc.nextLine()); ++i) {

        }
        for (int i = 0; i < Integer.parseInt(sc.nextLine()); ++i) {

        }
        while (sc.hasNextLine()) {

        }
    }

    public static void main(String[] args) throws FileNotFoundException {

        Scanner sc = new Scanner(new File("BinaryCardTree.txt"));

        BinaryCardTree tree = new BinaryCardTree();

        read(sc, tree);

        sc.close();
    }
}
